function stringLength(text){
    for (let index = text.length; index >= 0; index--) {
        const element = array[index];
        console.log(element);
    }
}
const array = "I am a good boy"
const rebarseText = stringLength(array)
console.log(rebarseText);